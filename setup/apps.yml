trame:
  cmd:
    - python
    - /deploy/viewervtu.py
    - --data_url
    - ${dataUrl}

    # - --port
    # - ${port}
    # - --host
    # - ${host}
    # - --authKey
    # - ${secret}
    # - --server
