trame:
  cmd:
    - python
    - /deploy/viewervtu.py
    - --data_url
    - ${DATA_URL}

    # - --port
    # - ${port}
    # - --host
    # - ${host}
    # - --authKey
    # - ${secret}
    # - --server
