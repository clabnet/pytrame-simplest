trame:
  cmd:
    - python
    - /deploy/viewervtu.py
    - --dataurl
    - ${dataurl}

    # - --port
    # - ${port}
    # - --host
    # - ${host}
    # - --authKey
    # - ${secret}
    # - --server
